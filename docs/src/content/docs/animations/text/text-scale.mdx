---
title: text-scale
description: Per-character vertical scale entrances. Each character grows from a flat line at its origin to its full height. Three origins ship out of the box — center, bottom, and top.
---

import Demo from '../../../../components/Demo.astro'
import TextDemo from '../../../../components/TextDemo.astro'

The scale family splits the element into characters and tweens `scaleY` from `0` to `1`. The transform origin decides which edge stays put: `text-scale` opens from the vertical center outward, `text-scale-up` grows upward from the baseline, and `text-scale-down` grows downward from the cap line.

## Variants

| Variant            | Transform origin | Visual effect                                |
| ------------------ | ---------------- | -------------------------------------------- |
| `text-scale`       | `50% 50%`        | Each char opens from its vertical center.    |
| `text-scale-up`    | `50% 100%`       | Each char grows upward from the bottom edge. |
| `text-scale-down`  | `50% 0%`         | Each char grows downward from the top edge.  |

## Attributes

| Attribute     | Default     | Notes                                                                  |
| ------------- | ----------- | ---------------------------------------------------------------------- |
| `aa-animate`  | (required)  | One of the variants above.                                             |
| `aa-split`    | `chars`     | Override split mode (`chars`, `words`, `lines`). Add `mask` flag for line clipping (see [descender padding](/animations/utilities/aa-split/#descender-padding)). |
| `aa-stagger`  | `0.02`      | Seconds between parts. Append `center` / `edges` / `random` to reorder. Falls back to `init({ stagger: { chars } })` (default `0.02`). |
| `aa-duration` | (init)      | Seconds. Falls back to `init({ duration })` (default `0.6`).           |
| `aa-delay`    | `0`         | Seconds.                                                               |
| `aa-ease`     | (init)      | Any GSAP ease. Falls back to `init({ ease })` (default `power4.out`).  |
| `aa-scrub`    | (off)       | `true` or seconds — bind progress to scroll.                           |
| `aa-color`    | (off)       | Accent colour each character pulses through (base → accent → hold → base) as it scales in. Any CSS colour or a `--custom-prop` name. Same accent wave as the [fade family](/animations/text/text-fade/#accent-colour-wave). |

## Required GSAP plugins

`gsap`, `ScrollTrigger`, `SplitText`.

## Live demo

Scale animations read well when they're snappy — these demos shorten the duration to `0.4s` and tighten the stagger to `0.015s`. The first row staggers from the center out; the up/down rows use the default stagger order.

<Demo caption='aa-animate="text-scale" aa-duration="0.4" aa-stagger="0.015 center"'>
  <div data-demo-stage>
    <TextDemo animate="text-scale" duration="0.4" stagger="0.015 center" />
  </div>
</Demo>

<Demo caption='aa-animate="text-scale-up" aa-duration="0.4" aa-stagger="0.015"'>
  <div data-demo-stage>
    <TextDemo animate="text-scale-up" duration="0.4" stagger="0.015" />
  </div>
</Demo>

<Demo caption='aa-animate="text-scale-down" aa-duration="0.4" aa-stagger="0.015"'>
  <div data-demo-stage>
    <TextDemo animate="text-scale-down" duration="0.4" stagger="0.015" />
  </div>
</Demo>

## Accent colour wave

Add `aa-color` to give each character an accent-colour pulse as it scales in — the same wave described on the [fade page](/animations/text/text-fade/#accent-colour-wave). Each character rests in its base colour, takes on the accent as it grows, holds it briefly, then settles back.

<Demo caption='text-scale accent wave'>
  <div data-demo-stage>
    <TextDemo animate="text-scale" color="#F84131" duration="0.4" stagger="0.015 center" />
  </div>
</Demo>
